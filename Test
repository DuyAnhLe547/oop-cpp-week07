#include <iostream>
#include <string>
using namespace std;



struct computer{
    int id;
    string name;
    string hdh;
    string username;
    int pwd;
    string model;
    int year;


};

struct lab{
    int maphong;
    computer a[50];
};

void nhapLab(lab &x, int n) {
    cout << "nhap ma phong: ";
    cin >> x.maphong;

    for (int i = 0; i < n; i++) {
        cout << "\ncomputer " << i + 1 << endl;

        cout << "id: ";
        cin >> x.a[i].id;

        cout << "name: ";
        cin >> x.a[i].name;

        cout << "hdh: ";
        cin >> x.a[i].hdh;

        cout << "username: ";
        cin >> x.a[i].username;

        cout << "pwd: ";
        cin >> x.a[i].pwd;

        cout << "model: ";
        cin >> x.a[i].model;

        cout << "year: ";
        cin >> x.a[i].year;
    }
}

void xuatLab(lab x, int n) {
    cout << "\nma phong: " << x.maphong << endl;

    for (int i = 0; i < n; i++) {
        cout << "\ncomputer " << i + 1 << endl;

        cout << "id: " << x.a[i].id << endl;
        cout << "name: " << x.a[i].name << endl;
        cout << "hdh: " << x.a[i].hdh << endl;
        cout << "username: " << x.a[i].username << endl;
        cout << "pwd: " << x.a[i].pwd << endl;
        cout << "model: " << x.a[i].model << endl;
        cout << "year: " << x.a[i].year << endl;
    }
}

void getcomputerInfo1(computer x[], int n){
     for (int i = 0; i < n; i++)
    {
        cout << "\nflower " << i + 1 << endl;

        cout << "id: ";
        cin >> x[i].id;

        cout << "name: ";
        cin >> x[i].name;

        cout << "hdh: ";
        cin >> x[i].hdh;

        cout << "username: ";
        cin >> x[i].username;

        cout << "pwd: ";
        cin >> x[i].pwd;

        cout << "model: ";
        cin >> x[i].model;

        cout << "year: ";
        cin >> x[i].year;


        
    }
}

void getcomputerInfo2(int id, int n, computer x[]){
     for (int i = 0; i < n; i++)
    {
       if(x[i].id == id){
        cout << "\nflower " << i + 1 << endl;
        cout << x[i].name;
        cout << x[i].hdh;
        cout << x[i].username;
        cout << x[i].pwd;
        cout << x[i].model;
        cout << x[i].year;
       }
    }
}

computer getcomputerInfo(int id, int n, computer x[]){
    for (int i = 0; i < n; i++)
    {
       if(x[i].id == id){
        return x[i];
       }
    }
}

int main(){
    computer x[50];
    int n;
    cout << "nhap n: ";
    cin >> n;

    lab L;

    cout << "nhap lab: ";
    nhapLab(L, n);

    cout << "xuat lab: ";
    xuatLab(L, n);

   

    getcomputerInfo1(x, n);

    int id;
    cout <<"nhap id: ";
    cin >> id;
    getcomputerInfo2(id, n, x);

    computer a = getcomputerInfo(id, n, x);
        cout << a.name;
        cout << a.hdh;
        cout << a.username;
        cout << a.pwd;
        cout << a.model;
        cout << a.year;

    
    

}