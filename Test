#include <iostream>
#include <string>
using namespace std;



struct computer{
    int id;
    string name;
    string hdh;
    string username;
    int pwd;
    string model;
    int year;


};

struct lab{
    int maphong;
    string chucnang;
    computer a[50];
};

void showlabinfo(lab &L) {
  lab a;

  cout << "Ma phong: ";
  cin >> a.maphong;

  cout << "Chuc nang: ";
  cin >> a.chucnang;
};


struct taikhoan {
  string username[50];
  string password[50];
};



void inputcomputerInfo(computer x[], int n){
     for (int i = 0; i < n; i++)
    {
        cout << "\nflower " << i + 1 << endl;

        cout << "id: ";
        cin >> x[i].id;

        cout << "name: ";
        cin >> x[i].name;

        cout << "hdh: ";
        cin >> x[i].hdh;

        cout << "username: ";
        cin >> x[i].username;

        cout << "pwd: ";
        cin >> x[i].pwd;

        cout << "model: ";
        cin >> x[i].model;

        cout << "year: ";
        cin >> x[i].year;


        
    }
}

void getcomputerInfo1(int id, int n, computer x[]){
     for (int i = 0; i < n; i++)
    {
       if(x[i].id == id){
        cout << "\nflower " << i + 1 << endl;
        cout << x[i].name;
        cout << x[i].hdh;
        cout << x[i].username;
        cout << x[i].pwd;
        cout << x[i].model;
        cout << x[i].year;
        return;
       }
    }
    cout << "Khong tim thay computer\n";
}

computer getcomputerInfo(int id, int n, computer x[]){
    for (int i = 0; i < n; i++)
    {
       if(x[i].id == id){
        return x[i];
       }
    }
    computer a;
    a.id = -1;
    a.name = "Khong tim thay";

    return a;
}

computer* getcomputers(computer x[]) {
    return x;
}

void gettaikhoan(int id, int n, computer x[]) {
    for (int i = 0; i < n; i++) {
        if (x[i].id == id) {
            cout << "Username: " << x[i].username << endl;
            cout << "Password: " << x[i].pwd << endl;
            return;
        }
    }

    cout << "Khong tim thay computer!\n";
}

int main(){
    computer x[50];
    int n;

    cout << "nhap n: ";
    cin >> n;


    
    lab L;

    cout << "\n=== NHAP LAB ===\n";
    showlabinfo(L);


    // Nhap computer
    cout << "\n NHAP COMPUTER \n";
    inputcomputerInfo(x, n);


    // 1. Tim va in computer theo ID
    int id;

    cout << "\nNhap id can tim: ";
    cin >> id;

    getcomputerInfo1(id, n, x);


    // 2. Tim va tra ve computer
    cout << "\nNhap id de tra ve Computer: ";
    cin >> id;

    computer a = getcomputerInfo(id, n, x);

    cout << "\n=== COMPUTER DUOC TRA VE ===\n";
    cout << "Name: " << a.name << endl;
    cout << "Model: " << a.model << endl;
    cout << "Year: " << a.year << endl;


    // 3. Tra ve con tro mang
    cout << "\n=== DANH SACH COMPUTER ===\n";

    computer* list = getcomputers(x);

    for (int i = 0; i < n; i++) {
        cout << i + 1 << ". ";
        cout << "ID: " << list[i].id << " | ";
        cout << "Name: " << list[i].name << " | ";
        cout << "HDH: " << list[i].hdh << endl;
    }


    
    cout << "\nNhap id de lay tai khoan: ";
    cin >> id;

    gettaikhoan(id, n, x);
    
    
    return 0;
}